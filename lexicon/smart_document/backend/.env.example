# OpenAI-compatible API Configuration
# Supports OpenAI, Groq, Together AI, or any OpenAI-compatible endpoint
OPENAI_API_KEY=your_api_key_here
OPENAI_MODEL=gpt-4-turbo
OPENAI_BASE_URL=

# Optional: API Key for endpoint protection (leave empty to disable)
API_KEY=

# Supabase Configuration (Optional — falls back to local file storage if empty)
SUPABASE_URL=
SUPABASE_KEY=
